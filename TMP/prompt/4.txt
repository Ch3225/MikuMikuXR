很好！那么现在我们来解决一个非常困难的问题。。。
你看这个界面哈。第二张是他加载时候的效果（模型和动作还未配置）。
现在我想把整个功能做出来。我们预想的功能虽然不是很多但是却非常复杂：例如，所有列表中的元素可以通过拖拽排序（我刚刚做了一个GenericItemListInserter的Prefab，我希望在拖动时能够在即将插入的地方有个预览，而预览就通过将这个prefab放到将要插入的为止来实现），所有列表中的元素可以通过拖拽到某个Panel进行删除。Music列表和Camera的列表有一个恒定的激活项（我们原本打算将列表的首项作为固定的激活项，拖拽到列表顶部即可，现在我改变注意了，激活项不一定是首项，但只要列表里有东西，一个列表必须有一个激活项）。对于Models和Motions我们可以通过拖拽一个card到另一个card上来实现对model和motion进行关联（哦！我们的存储好像还没做完。我们好像没有单独存储模型和动作的关联。它应当是一个Map<模型,List<动作>>这样，存储在SceneManager里边；又或者你给ModelConponent里边添加一个引用动作的列表）。然后这种关联可以通过将Models或者Motions的元素拖拽到对方的某一个元素上建立，或者将元素拖拽到那个Disconnect 的panel上取消这个元素的所有关联。对于Enable和Disable功能，我原本计划仅对模型生效，模型可以disable让它暂时消失不见。现在我希望额外的功能（虽然我还没添加描述），就是把music和camera的item拖过去的话能把被拖得那个作为正在激活的那一项（但是不能取消激活）。
我们的要求非常困难，目前ai给我生成了以下部分的代码，并且它们的功能存在问题，我将参考截图3说明我们加载代码的结构：
DropzoneUnload挂载Assets/MMDVR/Scripts/UIInteraction/DropZone.cs，其中OnItemDropped未能配置完成，按理说我们要配置四个，目前我仅仅配置了MusicListController.handleDropOnUninstallZone，恐怕我们还要配置其余三个；而且AcceptedResourceTypes很奇怪，按理说我们Actor和Model都是一回事。另外我还感觉哪里怪怪的。DropzoneEnabling和DropzoneDisconnect均只配置了Dropzone的script而没做任何事情。
MusicListPanel挂载了Assets/MMDVR/Scripts/UIInteraction/MusicListController.cs，Camera挂载了Assets/MMDVR/Scripts/UIInteraction/CameraListController.cs，而Model和Motion的没挂载（因为还没写）MusicList和CameraList配置了Dropzone，截图4展示了musiclist的，cameralist的类似。
现在我希望你帮我初步生成所有的代码来实现这些功能，有时候由于我的要求有变化，你可能也要修改原有的功能。在修改完成之后，你可能需要告诉我如何配置这些。



我计划做一个VR下的mmd播放器。你现在所看到的是MikuMikuXR这一项目经过了一点魔改，但我们并不打算做安卓，而是做windows平台，另外我们也不做增强现实的应用，而原项目使用了一些增强现实的库。
注意代码非常多，在解决问题时请仅查看必要的代码，否则会超过这次问答的上下文导致你崩溃。
我已经用UGUI做出了一套UI，之后我们可能还会改。只是我是Unity新手，所以很多操作不熟悉，现在我打算为我的UI增添逻辑，你需要写出对应的操作步骤。对于mmd相关的模型的加载，控制等请参见Assets_Packs\LibMmd。也可以参考Assets/MMDVR/Managers/SceneStatesManager.cs的引用和间接引用，这是原有的已被废弃的读取界面所引用的内容。
现在我们可以实现拖拽Draggable item到对应的位置Dropzone来实现不同的功能。可是现在还有一些功能未实现。比如将模型或者动作拖拽到Action Type为Disconnect的Drop zone。现在我希望你实现如下效果：
如果拖拽模型到Disconnect：清理模型和所有关联的动作，并加载空动作；
如果拖拽动作到Disconnect：对所有的模型在动作列表中删除该动作，并使得模型重新加载动作列表。
对于Uninstall对模型的动作的处理，虽然原有的代码已经写出来，但确保以下逻辑：在拖拽取消连接的基础上，删除所拖拽draggable item所对应的资源。


